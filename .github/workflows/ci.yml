name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test-zsh:
    name: Test Zsh on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Zsh (Ubuntu)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y zsh

      - name: Run Zsh Test Suite
        run: |
          chmod +x test-sharmory.zsh
          ./test-sharmory.zsh

  test-bash:
    name: Test Bash on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Bash 4+ (macOS ships Bash 3.2)
        if: runner.os == 'macOS'
        run: brew install bash

      - name: Run Bash Test Suite
        run: |
          chmod +x test-sharmory.bash
          ./test-sharmory.bash

  test-windows:
    name: Test PowerShell on Windows
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run PowerShell Test Suite (Windows PowerShell 5.1)
        shell: powershell
        run: |
          .\test-sharmory.ps1

      - name: Run PowerShell Test Suite (PowerShell Core 7+)
        shell: pwsh
        run: |
          .\test-sharmory.ps1
